name: ngajidong
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  assets_audio_player: ^3.1.1
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.2.0
  svg_flutter: ^0.0.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
  - assets/data/
  - assets/svg/
